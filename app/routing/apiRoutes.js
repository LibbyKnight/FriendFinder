var friend = require('../data/friends.js');

var path = require('path');



module.exports = function(app){
	for (i = 0; i < friendArray.length; i++) {
		
		var friend = friendArray[i].scores;
		var user = userArray.scores;
		
		for (i = 0; i < friend; i++) {

			
		}
	}
	
};
